---
description: "Comment style conventions for code comments"
globs: "**/*.{rs}"
alwaysApply: true
---

# Overview

Every sentence in comments must start with a capital letter and end with a period. Applies to line comments (`//`), doc comments (`///`), and block comments (`/* */`).

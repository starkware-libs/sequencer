[tool.black]
exclude = '(/|\\)imports'
line-length = 100
target-version = ['py310', 'py311', 'py312']

[tool.isort]
known_third_party = ["cdk8s", "constructs"]
line_length = 100
profile = "black"
skip = ["imports"]

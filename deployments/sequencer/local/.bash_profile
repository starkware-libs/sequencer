# ~/.bash_profile: executed by bash(1) for login shells.
# If .bash_profile exists, bash doesn't automatically read .bashrc
# So we source it here to get all our aliases and PATH settings

if [ -f ~/.bashrc ]; then
    . ~/.bashrc
fi

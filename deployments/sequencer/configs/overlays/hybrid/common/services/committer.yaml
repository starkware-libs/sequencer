name: committer
config:
  configList: crates/apollo_deployments/resources/services/hybrid/replacer_deployment_committer.json

service:
  enabled: true
  type: ClusterIP
  ports:
  - name: committer
    port: 55013
    targetPort: 55013
    protocol: TCP

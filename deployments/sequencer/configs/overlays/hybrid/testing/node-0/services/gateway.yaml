include:
  - configs/overlays/hybrid/testing/node-0/common.yaml

name: gateway

replicas: 1

service:
  enabled: true
  type: ClusterIP
  ports:
  - name: gateway
    port: 55002
    targetPort: 55002
    protocol: TCP
  - name: http-server
    port: 8080
    targetPort: 8080
    protocol: TCP

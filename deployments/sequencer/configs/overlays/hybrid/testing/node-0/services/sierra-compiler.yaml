include:
  - configs/overlays/hybrid/testing/node-0/common.yaml

name: sierracompiler

replicas: 1

config:
  configList: crates/apollo_deployments/resources/services/hybrid/replacer_deployment_sierra_compiler.json

service:
  enabled: true
  type: ClusterIP
  ports:
  - name: sierra-compiler
    port: 55007
    targetPort: 55007
    protocol: TCP

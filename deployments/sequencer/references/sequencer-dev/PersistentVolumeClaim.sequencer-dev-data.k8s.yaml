apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: sequencer-dev
  name: sequencer-dev-data
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 64Gi
  storageClassName: premium-rwo
  volumeMode: Filesystem

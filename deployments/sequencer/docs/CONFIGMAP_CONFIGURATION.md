# ConfigMap Configuration Guide

This document describes the `config` (ConfigMap) options for the sequencer cdk8s constructs.

## Overview

Each node service's ConfigMap holds a **single, fully-resolved `SequencerNodeConfig`** as nested JSON. The
config is generated by evaluating the service's leaf overlay `node.jsonnet` through the jsonnet `build()`
evaluator — you do **not** assemble it from a list of files. The resulting JSON is stored under the
ConfigMap data key `config` and mounted as a file for the node to load.

The node uses the **native** config loader, which takes exactly **two** `--config_file` arguments:

1. the **base** config — this ConfigMap (a complete, resolved nested config), and
2. a **secrets** overlay — provided by a Secret / ExternalSecret (see
   [SECRET_CONFIGURATION.md](./SECRET_CONFIGURATION.md) and
   [EXTERNAL_SECRET_CONFIGURATION.md](./EXTERNAL_SECRET_CONFIGURATION.md)).

Both arguments are added automatically by the pod builder.

## Basic Configuration

`config` is **required** for every node service. With no options set, the ConfigMap is generated from the
service's leaf overlay `node.jsonnet` and mounted at the default path:

```yaml
config: {}
```

## Configuration Options

### `config` (object)
- **Required**: Yes
- **Description**: Enables ConfigMap generation and mounting. The content is the nested
  `SequencerNodeConfig` produced by jsonnet `build()` for the service's overlay.

#### `mountPath` (string, optional)
- **Default**: `/config/sequencer/presets/`
- **Description**: Directory the ConfigMap is mounted at. The config file lands at `<mountPath>/config` (a
  missing trailing slash on `mountPath` is added automatically).

#### `readOnly` (bool, optional)
- **Default**: `true`
- **Description**: Whether the ConfigMap volume mount is read-only.

#### `sequencerConfig` (object, deprecated)
- **Description**: Ignored. Earlier versions deep-merged this block into the generated config; the ConfigMap
  now comes entirely from `node.jsonnet`, so every key set here is dropped and `cdk8s synth` logs a warning
  naming it. Existing overlays may keep `sequencerConfig: {}`. Put public values in the overlay's
  `chain_params.jsonnet` and private ones in the sequencer-devops `node.jsonnet` /
  `private_chain_params.jsonnet`.

## Where the config comes from

The ConfigMap content is produced by evaluating the `node.jsonnet` of the **last** `-o/--overlay`
argument, with the dots in the overlay name mapped to directories:

```
cdk8s synth --layout hybrid -o hybrid.mainnet -o hybrid.mainnet.apollo-mainnet-0
→ deployments/sequencer/configs/jsonnet/overlays/hybrid/mainnet/apollo-mainnet-0/node.jsonnet
```

`node.jsonnet` imports `lib/build.libsonnet` (directly, or through the env's `private_chain_params.jsonnet`)
and emits the full per-service `SequencerNodeConfig`. The `node.jsonnet` files committed in this repo under
`configs/jsonnet/overlays/hybrid/<env>/` are CI stand-ins (dummy validator id, no bootstrap peers). For a
real deployment, sequencer-devops supplies the per-node `node.jsonnet`, and the deploy workflow must place
it at that same path under `configs/jsonnet/overlays/`. There is no `configPaths` list and no
`resources/app_configs/*.json` files — those belonged to the retired preset config path.

## Generated Kubernetes Resource

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: sequencer-<service>-config
  labels:
    app: sequencer
    service: sequencer-<service>
data:
  # The whole nested SequencerNodeConfig under a single "config" key (not "config.json").
  config: |
    {
      "components": { "batcher": { "execution_mode": "Enabled" }, ... },
      "batcher_config": { ... },
      "consensus_manager_config": { ... },
      "http_server_config": { ... },
      ...
    }
```

The data key is `config` (not `config.json`); it is mounted as a file named `config`.

## Mounting in Pods

The ConfigMap is mounted automatically when `config` is set:

```yaml
volumeMounts:
  - name: sequencer-<service>-config
    mountPath: /config/sequencer/presets/   # or custom mountPath
    readOnly: true
```

## Automatic Container Arguments

The pod builder always adds the base config as the first `--config_file`, and — when a Secret or
ExternalSecret is configured — the secrets file as the second `--config_file`:

```yaml
args:
  - --config_file
  - /config/sequencer/presets/config   # base: this ConfigMap (or <mountPath>config)
  - --config_file
  - /etc/secrets/secrets.json          # secrets: ExternalSecret (or /etc/secrets/secret.json for a Secret)
```

The native loader requires **exactly two** `--config_file` arguments (base + secrets). One argument (a
ConfigMap with no Secret/ExternalSecret) and three arguments (both `secret` and `externalSecret` enabled)
both pass `cdk8s synth` and fail at node start — enable exactly one secrets source (an empty secrets file
is acceptable).

## Inspecting what a node will load

```bash
cd deployments/sequencer
cdk8s synth --layout hybrid -o hybrid.mainnet -o hybrid.mainnet.apollo-mainnet-0
# one ConfigMap manifest per service lands in dist/; the JSON the node loads is its data.config value
yq '.data.config' dist/<manifest>.yaml | jq .
```

## Best Practices

1. **jsonnet is the source of truth**: change config through the overlay `node.jsonnet` / `chain_params`,
   not by hand-editing the generated JSON.
2. **Keep secrets out of the ConfigMap**: sensitive values belong in a Secret / ExternalSecret, loaded as
   the second `--config_file`.
3. **Do not use `sequencerConfig`**: it is ignored; change `chain_params` / `node.jsonnet` instead.
4. **Always provide a secrets source**: the native loader needs two config files, so a ConfigMap alone will
   not boot the node.

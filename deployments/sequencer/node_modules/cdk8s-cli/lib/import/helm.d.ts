import { CodeMaker } from 'codemaker';
import { ImportBase } from './base';
import { ImportSpec } from '../config';
export declare class ImportHelm extends ImportBase {
    static fromSpec(importSpec: ImportSpec): Promise<ImportHelm>;
    private readonly chartName;
    private readonly chartUrl;
    private readonly chartVersion;
    private readonly chartSchemaPath;
    private readonly chartDependencies;
    private readonly schema;
    private constructor();
    get moduleNames(): string[];
    protected generateTypeScript(code: CodeMaker): Promise<void>;
}

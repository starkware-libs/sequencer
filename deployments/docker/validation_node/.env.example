# =============================================================================
# Validation-Only Node - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in all values before running docker compose.

# Docker image settings
IMAGE_TAG=latest
IMAGE_REGISTRY=ghcr.io/starkware-libs/sequencer

# Logging
RUST_LOG=info
RUST_BACKTRACE=1

# P2P networking
# Must match the value of <CONSENSUS_P2P_PORT> in config/validation_node.json
CONSENSUS_P2P_PORT=10000

# Data directory (host path for persistent storage)
DATA_DIR=./data

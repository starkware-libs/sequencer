[tool.poetry]
description = "Monitoring dashboards and alerts (Grafana)"
name = "sequencer-monitoring"
package-mode = false
version = "0.1.0"

[tool.poetry.dependencies]
grafana-client = "4.3.2"
python = "^3.10"
pyyaml = ">=6.0"
requests = "2.32.3"
rich = ">=13.0.0"
tenacity = "9.1.2"

[tool.poetry.group.dev.dependencies]

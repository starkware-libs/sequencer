name: Main-CI-PR-Flow

on:
  merge_group:
  pull_request:
    types:
      - opened
      - reopened
      - edited
      - synchronize

env:
  RUSTFLAGS: "-D warnings"

# On PR events, cancel existing CI runs on this same PR for this workflow.
# Also, create different concurrency groups for different pushed commits, on push events.
concurrency:
  group: >
    ${{ github.workflow }}-
    ${{ github.ref }}-
    ${{ github.event_name == 'pull_request' && 'PR' || github.sha }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  commitlint:
    # An edit only matters where the PR title is read. github.event.changes names the
    # field that changed, so a body-only edit skips instead of re-running the suite.
    if: ${{ github.event.action != 'edited' || github.event.changes.title != null }}
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 0

      - name: Install commitlint
        run: npm install --global @commitlint/cli @commitlint/config-conventional

      - name: Validate PR title with commitlint
        if: github.event_name != 'merge_group' && github.event_name != 'push' && !(contains(github.event.pull_request.title, 'merge-main') || contains(github.event.pull_request.title, 'merge main'))
        env:
          TITLE: ${{ github.event.pull_request.title }}
        run: echo "$TITLE" | commitlint --verbose

  # This job is used to check if all checks have passed before merging the PR.
  # Uses starkware-libs/merge-gatekeeper as the implementation.
  merge-gatekeeper-new:
    # An edit only matters where the PR title is read. github.event.changes names the
    # field that changed, so a body-only edit skips instead of re-running the suite.
    if: ${{ github.event.action != 'edited' || github.event.changes.title != null }}
    runs-on: ubuntu-24.04
    timeout-minutes: 75
    permissions:
      actions: read
      checks: read
      statuses: read
    steps:
      - name: Run merge-gatekeeper-new on pull request
        if: github.event_name == 'pull_request'
        uses: starkware-libs/merge-gatekeeper@c0535e7c50a3fc70a0f4857afd37ccd6cfcc59de # v1
        with:
          self: merge-gatekeeper-new
          timeout: 3600
          interval: 30
          ignored: "code-review/reviewable,build_docker_images"

      - name: Run merge-gatekeeper-new on Merge Queue || push
        if: github.event_name == 'merge_group' || github.event_name == 'push'
        uses: starkware-libs/merge-gatekeeper@c0535e7c50a3fc70a0f4857afd37ccd6cfcc59de # v1
        with:
          self: merge-gatekeeper-new
          ref: ${{github.ref}}
          timeout: 1800
          interval: 30
          ignored: "code-review/reviewable"

name: Idan Test Workflow

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main-v0.14.0

jobs:
  test:
    runs-on: blacksmith-2vcpu-ubuntu-2404
    steps:
      - run: echo "Hello World"

[tool.poetry]
name = "sequencer-system-tests"
version = "0.1.0"
description = "Python deps for scripts/system_tests (readiness, liveness, etc.)"
package-mode = false

[tool.poetry.dependencies]
python = "^3.10"
kubernetes = "*"
requests = "*"
pyyaml = "*"

[tool.poetry.group.dev.dependencies]

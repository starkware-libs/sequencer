apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: dummy-200-app
    files:
      - app.py=../../app/app.py
      - block_storage.py=../../app/block_storage.py

generatorOptions:
  disableNameSuffixHash: true




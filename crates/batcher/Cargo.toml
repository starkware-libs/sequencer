[package]
name = "starknet_batcher"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
blockifier = { path = "../blockifier", version = "0.8.0-rc.0"}
starknet_gateway = { path = "../gateway" }

[dev-dependencies]
mockall.workspace = true

[lints]
workspace = true
